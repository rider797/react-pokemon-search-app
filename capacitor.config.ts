import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pokemon.app',
  appName: 'react-pokemon-search',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
